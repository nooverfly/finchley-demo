spring:
  application:
    name: common-service-zuul
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-center
      label: master
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/